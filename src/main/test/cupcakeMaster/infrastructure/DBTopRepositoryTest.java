package cupcakeMaster.infrastructure;

import static org.junit.jupiter.api.Assertions.*;

class DBTopRepositoryTest {
    Database db = new Database();
    DBTopRepository repo=new DBTopRepository(db);
    repo.
}
